# ============================================================================
# BetVector — Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit the real .env file to version control.
#
# These variables are read by the pipeline (run_pipeline.py) and the
# Streamlit dashboard (src/delivery/dashboard.py).
# ============================================================================

# --- Dashboard Access ---
# Simple password gate for the Streamlit dashboard.
# Leave empty to disable the password gate (useful for local dev).
DASHBOARD_PASSWORD=

# --- Email Alerts ---
# The Gmail address that BetVector sends emails from (and to).
FROM_EMAIL=

# Gmail App Password for the above email address.
# Generate one at: https://myaccount.google.com/apppasswords
# (Requires 2FA enabled on your Google account.)
GMAIL_APP_PASSWORD=

# --- API Keys ---
# RapidAPI key for API-Football (free tier: 100 requests/day).
# Sign up at: https://www.api-football.com/
API_FOOTBALL_KEY=

# Football-Data.org API key (free tier: 10 requests/minute).
# Sign up at: https://www.football-data.org/client/register
FOOTBALL_DATA_ORG_KEY=

# The Odds API key (free tier: 500 requests/month).
# Provides pre-match odds from 50+ bookmakers including Pinnacle and FanDuel.
# Sign up at: https://the-odds-api.com/
THE_ODDS_API_KEY=

# odds-api.io API key (free tier: 100 requests/hour, no monthly cap).
# Supplementary odds source — automatic fallback when The Odds API budget
# is exhausted. Covers all 6 BetVector leagues with 250+ bookmakers.
# Sign up at: https://odds-api.io/ (no credit card required)
ODDS_API_IO_KEY=

# --- Cloud Database (future) ---
# PostgreSQL connection string for cloud deployment (Neon, Supabase, etc.).
# When set, sync_to_cloud.py pushes local SQLite data to this database.
# Leave commented out until cloud sync is configured.
# CLOUD_DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
