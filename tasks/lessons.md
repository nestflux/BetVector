# BetVector — Lessons Learned
# Format: [date] | what went wrong | rule to follow next time
# Read at session start. Auto-promoted to gates after 3 occurrences.

[2026-06-25] | Invite hardening: the Settings "Invite User" button called create_viewer_user() which created a user with password_hash=NULL — a login-less DEAD ACCOUNT (login falls through to "invalid credentials"). Meanwhile a SECOND creation path (Admin "Create User") set a password. Two paths for the same action, one silently broken, plus the success copy lied ("They can access the dashboard"). Also: there was no self-service password change anywhere — owner-assigned passwords were permanent. | An account-creation action named "Invite/Create user" MUST produce a USABLE login — if it doesn't set (or generate) a credential, it's a trap. When a single action has two entry points, exercise BOTH and assert the end state is identical and usable. For owner-assigned passwords, always pair with a forced first-login change (must_change_password flag, gated in dashboard.main() BEFORE onboarding) + a self-service change form, so the owner never permanently knows a user's password. Fix shipped: create_viewer_user now auto-generates a temp password (secrets, unambiguous alphabet), stores a real hash, returns the plaintext ONCE for the owner to share, and forces a change on first login.

[2026-06-25] | Neon Postgres hit its data-transfer quota mid-session ("Your project has exceeded the data transfer quota. Upgrade your plan") — the SAME failure as PC-13. This blocks BOTH the live cloud app (betvector.streamlit.app reads Neon) AND any ad-hoc migration against Neon. Earlier the same session it connected fine, so the cap tipped over during the day. | Treat a Neon free-tier quota wall as a recurring operational blocker (Rule 4: owner action — wait for the monthly reset or upgrade the plan). Mitigation that worked: the local SQLite backup is the dev DB the test suite imports against, so migrate THAT directly (independent create_engine, not get_engine which is singleton-bound to Neon) to unblock local dev + tests; Neon self-migrates via init_db()'s _apply_schema_migrations on the next reachable pipeline run (the migration entry is permanent and idempotent), so a manual Neon ALTER isn't required — just don't deploy schema-dependent code to cloud until Neon is reachable and has self-migrated. would shift the documented league TIERS — but the tiers are bootstrap CIs on PER-BET returns, which are STAKING-INVARIANT (staking changes the point ROI, not the CI). Confirmed by reproducing Championship's documented CI EXACTLY ([3.4,23.2] vs doc [3.5,23.0], 731 bets = doc 731). The real surprise was DATA DRIFT — re-running the OTHER leagues' tuned configs on CURRENT data gave wildly different numbers (LaLiga 110→16 bets: Pinnacle/sharp-only coverage collapsed; EPL Brier .578→.603 + heavy λ-clamp). | Before fearing a config/code change will move a documented "baseline", check whether that baseline is a POINT estimate (often depends on the thing you're changing) or a CONFIDENCE INTERVAL (often invariant to it). When re-running old backtests to measure ANY change, reproduce a KNOWN-STABLE case first — if it matches, the method is sound and divergence elsewhere is DATA DRIFT, not your change. Sharp-only/Pinnacle coverage can silently shrink over time; thin samples (16 bets) give meaningless CIs — don't re-baseline on them.

[2026-06-25] | Backtest ROI under COMPOUNDING flat staking (stake = running bankroll × %) is BIASED: when a strategy loses, the bankroll death-spirals toward $0 and stakes shrink with it, so losses stop accumulating and the apparent ROI is FLATTERED. Found when aligning the backtester to level staking — EPL backtest read −12.7% compounding vs −17.8% level; Ligue1 −27.4% vs −37.0%; deltas up to −9.5pp. | For backtest ROI use LEVEL staking (constant stake off the STARTING bankroll) — it's the honest per-unit edge and isn't path-dependent. Compounding makes ROI depend on the ORDER of wins/losses and dampens losses (flatters losers). ALWAYS sanity-check a backtest's final bankroll path: if it crashed to ~$0, the compounding ROI is meaningless. (Brier is staking-independent — unaffected.) Corollary: when measuring a change's effect, isolate it — here, replaying ONE backtest's realised bet sequence under both staking methods (same bets/results, only stake sizes differ) gave the clean delta without re-running or re-training.

[2026-06-25] | Staking copy-vs-code reconcile: HC-04 had "fixed" the copy to match the code (flat staking = % of CURRENT bankroll), which made flat IDENTICAL to percentage — two staking methods doing the same thing. But the masterplan's own onboarding design (§3 Flow 6: "calculate stakes as a percentage of THIS [starting] amount") showed the CODE was the drift, not the copy. | When copy and code disagree, don't reflexively "fix the copy to match the code" — check the design source of truth (masterplan) and the textbook definition first; the code may be the bug. A reconcile that leaves two features doing the identical thing is a smell — flag the degenerate design, don't document it as correct. (Owner chose to fix the CODE: flat now = level staking off the STARTING bankroll, distinct from percentage.) Also: reconcile EVERY source at once (live code + all inline UI copy + the shared help_content source + the masterplan/buildplan), or they drift apart again.

[2026-06-24] | HC-02 screen-tour: 6 of 11 cards described a badge/colour/control the page doesn't render (WC "trust mark" was really qualification dots; Match Deep Dive had no "★ best price"; Today's Picks showed no "✅/❌ + P&L"). | When authoring any text that describes the UI (a manual, tooltip, glossary, caption), cross-check EACH badge/colour/control claim against the real view file before shipping. For documentation issues, Gate 2 faithfulness is the load-bearing gate — open the views and grep for the actual marker, never assume from memory.

[2026-06-24] | HC-03/HC-01: the help FAQ AND the glossary "Edge" entry said value picks are flagged against a DE-VIGGED price, but value_finder.py flags on the RAW implied prob (implied=1/odds, vig included; edge=model−implied). De-vig is only a deep-dive DISPLAY refinement. The HC-01 glossary audit had picked the "de-vigged" wording for being "technically cleanest" without checking what the value finder actually does. | When documenting a core mechanic (how edge/picks/stakes are computed), verify against the ACTUAL code path that does it (value_finder.py / predictor.py), not just internal doc consistency or which wording sounds most correct. "Cleanest concept" ≠ "what the code does".

[2026-06-24] | HC-04 Betting 101: two threshold/parameter claims were wrong because they were copied from the app's OWN UI text, which itself disagreed with the code — drawdown alert said "~30%" but settings.yaml drawdown_alert_threshold=0.25 (25%); "flat staking = % of starting bankroll" but bankroll.py computes flat off current_bankroll (same as percentage). | For a teaching/reference page: (1) re-compute EVERY worked numeric example, and (2) verify every threshold/parameter against config + the function that uses it (config/*.yaml, bankroll.py, value_finder.py) — NOT against the app's existing UI copy, which can be stale/wrong. When the manual and the app's own copy disagree with the code, fix the source of truth and spawn a task to reconcile the rest.

[2026-06-25] | WC standings showed 1 game/team while reality had 2-3. Root cause: the WC results scraper reads The Odds API /scores with daysFrom=3 (a hard 3-day cap), and daily WC capture only started ~23 Jun — so every match from the 11 Jun opener through ~19 Jun (~32 matches) fell outside the rolling window and was NEVER ingested. A reboot couldn't fix it (the DB itself was missing the matches). | A results pipeline built on a short-lookback API (daysFrom<=3) is only COMPLETE if it has run continuously since day 1; otherwise it has permanent holes. Pair it with an unlimited-lookback source (ESPN's free scoreboard, by date) for a one-time backfill AND a per-run self-heal sweep, and verify completeness against the real schedule (games-played per team), not just "the scraper exited 0". Diagnose "stale dashboard" by querying the DB directly first — if the DB matches the dashboard, it's a data gap, not a cache/reboot issue.

[2026-06-25] | I changed a config VALUE (board_markets) in the 422 fix and committed without running the full suite — 2 tests that asserted the old value (test_wc_odds_refresh.py) failed silently until the next run caught them. | After ANY change to a config value (config/*.yaml) or a model default, run the FULL test suite before committing, not just a manual smoke — tests frequently assert exact config values, and a manual validation (scrape returned N rows) won't surface a broken assertion.

[2026-06-25] | WC odds 422: the once-daily WC board pull requested board_markets="h2h,totals,btts,alternate_totals" on The Odds API BULK endpoint (/sports/{sport}/odds) → 422 INVALID_MARKET ("markets not supported by this endpoint"), which fails the WHOLE request (loads nothing, not just the bad markets). NOT an availability issue. | The Odds API splits markets: "featured" (h2h, spreads, totals) are on the bulk /odds endpoint; "additional" (btts, alternate_totals, player props, etc.) are ONLY on the per-event /events/{id}/odds endpoint. Never put an additional market in a bulk-endpoint markets list. Fix: board_markets="h2h,totals" (the deep dive derives O/U 1.5/3.5 + BTTS from the model's scoreline grid anyway, DF-01). Validated: scrape_wc_odds loaded 1998 rows, no 422. (League side was already h2h,totals — unaffected.)

[2026-06-25] | WC standings diagnosis: I concluded "the WC pipeline hasn't run since 23-Jun" because max(WCMatch.updated_at) was the bulk-load time — WRONG. WCMatch.updated_at has server_default=func.now() but NO onupdate=func.now(), so it only records INSERT time and never reflects UPDATEs; the pipeline log actually showed "WC results: 36 updated" on 24-Jun. The real cause was the once-daily morning cadence (4 matches finished after the 09:30 scrape). | Never use a model's updated_at as a "last modified / pipeline ran" signal unless the column has onupdate=. Verify pipeline activity from the LOGS or a run-record table (pipeline_runs), and verify result freshness from domain state (status='finished' + a real result timestamp), not updated_at. (Latent nit: WCMatch.updated_at is effectively dead — could add onupdate later.)

[2026-06-25] | RBAC/multi-user: onboarding AND Settings let ANY user toggle GLOBAL state — League.is_active (which drives the shared data pipeline) and injury flags (which skew everyone's predictions) — because the app was built single-user. A tester could have switched off everyone's leagues. | When moving a single-user app to multi-user, audit EVERY write for global-vs-per-user scope: per-user writes must key on get_session_user_id(); global/system writes (league activation, injury flags, model config) must be owner-gated in EVERY entry point (Settings, onboarding, any wizard). Also gate ops/infra pages (Data Health) both in the nav AND in-page (defence in depth), mirroring the existing Admin pattern.

[2026-06-24] | EMAIL-OPTIN: to flip a NEW-user default (notify_morning/evening/weekly on→off) on an ALREADY-DEPLOYED DB, changing the model's `server_default` alone is NOT enough — server_default only applies at create_all (fresh schemas); the existing column keeps its old DB-level DEFAULT (1), so new ORM inserts would still get 1. | Add the Python-side `default=0` to the Column (alongside server_default): SQLAlchemy applies `default=` on every ORM insert and overrides the DB default, so new users come out off on the live DB with NO ALTER COLUMN (which SQLite can't do anyway). Verified by creating a probe user (rolled back) against the existing-schema local DB. Also remember UI creation paths (onboarding wizard toggles + their session_state fallbacks) carry their own defaults — flip those too, or the wizard re-opts the user in.

[2026-06-24] | WC-EMAIL: adding a NOT-NULL column (notify_wc) to the User model broke 11 e34 auth tests with "no such column: users.notify_wc" — because admin.py queries users at IMPORT time, and that hit the local data/betvector.db backup, whose schema predated the column (the migration only auto-applies when init_db runs). | When adding a column to any model, apply the migration to BOTH the local SQLite dev DB AND Neon immediately (run init_db with all model modules imported, once per DB as separate processes — the engine singleton caches the first URL). Import-time DB queries in views mean a stale local backup will fail the suite even though the ORM/migration are correct. The model + migration entry alone isn't enough; the live schemas must be migrated too.

[2026-06-24] | DH-01 data-health engine: the first version flagged league sources (predictions/results/stats/weather) as FAIL because they were weeks stale — but it was the June off-season, so that staleness was expected, not broken. The live smoke against Neon showed 4 spurious FAILs that would have made the owner distrust the tool. | A data-FRESHNESS check must be ACTIVITY-aware: gate "source is stale" on whether the source SHOULD be flowing right now (e.g. leagues in-season — any upcoming fixture or a match finished in the last few days), and SKIP rather than FAIL when it's legitimately idle. And ALWAYS smoke a new monitoring check against real production data before trusting its verdicts — false positives are how a health tool loses the owner's trust.

[2026-06-24] | HC-06 glossary migration: consolidating the WC deep-dive "Anytime scorer %" def into the shared master dropped the qualifier "adjusted" ("goal-share of the team's expected goals" vs the original "...adjusted expected goals") — but the scorer board feeds the ADJUSTED λ (research.py: per-player exp_goals sums to lambda_adjusted), so the shorter wording was less precise. Gate 2 caught it. | When centralising duplicated copy into a single source, diff the OLD text against the new shared definition term-by-term (not just "same concept") — a consolidation can silently drop a qualifier the code actually depends on. Behaviour-preserving means the rendered MEANING is preserved, including precision, not just the term list.
